# calender
a calender application to add events, remainders. etc using react.
 the code is in master branch.
 the deployed link is . 
 to run the code in local environment :-
 1) clone the repository in local machine.
 2) install node modules.
 3) install tailwindcss.
 4) install js library dayjs.
 5) type npm run dev.
